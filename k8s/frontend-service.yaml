apiVersion: v1
kind: Service
metadata:
  name: frontend-despacho
  namespace: despacho
  labels:
    app: frontend-despacho
spec:
  type: LoadBalancer
  selector:
    app: frontend-despacho
  ports:
    - name: http
      port: 80
      targetPort: 80
